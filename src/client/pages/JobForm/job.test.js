// import React from "react";
// import { shallow } from "enzyme";
// import Job from "./JobFormPage";

test("component should render without errors", () => {
    // const wrapper = shallow(<Job />);
    // expect(wrapper.length).toBe(1);
});
